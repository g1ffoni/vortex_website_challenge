/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * UniforTrade API
 * Marketplace de economia circular do campus — estudantes anunciam itens (livros, calculadoras, componentes eletrônicos, jalecos, móveis etc.) para venda ou doação a outros estudantes.
 *
 * Fluxo de uma transação: uma listing ACTIVE é reivindicada (`POST /{id}/claim`, vira CLAIMED), as duas partes trocam contato (`GET /{id}/contact`) para combinar local e horário, e o vendedor confirma a entrega presencial (`POST /{id}/complete`). Qualquer uma das partes pode desistir antes da entrega (`DELETE /{id}/claim`), o que devolve a listing para ACTIVE.
 *
 * A maioria dos endpoints de leitura é pública; escrita e ações de fluxo exigem um usuário autenticado, e endpoints de categoria (além de GET) exigem a role ADMIN.
 *
 * Autenticação: o cadastro é `POST /api/users`, que cria a conta e retorna o perfil, mas não devolve tokens. Faça login em `POST /auth/login` para receber o par `accessToken` / `refreshToken`, renove em `POST /auth/refresh` e encerre a sessão no cliente com `POST /auth/logout`. Use o `accessToken` no botão "Authorize" acima para enviá-lo como `Authorization: Bearer <token>`.
 *
 * OpenAPI spec version: 0.0.1
 */
import useSwr from 'swr';
import type {
  Key,
  SWRConfiguration
} from 'swr';

import useSWRMutation from 'swr/mutation';
import type {
  SWRMutationConfiguration
} from 'swr/mutation';

import type {
  CreateRatingRequest,
  RatingResponse,
  UserRatingsResponse
} from '../models';

import { apiFetch } from '../../mutator';



  type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];



 export const findByUserId = (
    userId: number,
 options?: SecondParameter<typeof apiFetch>) => {
    return apiFetch<UserRatingsResponse>(
    {url: `/api/users/${userId}/ratings`, method: 'GET'
    },
    options);
  }



export const getFindByUserIdKey = (userId: number,) => [`/api/users/${userId}/ratings`] as const;

export type FindByUserIdQueryResult = NonNullable<Awaited<ReturnType<typeof findByUserId>>>

export const useFindByUserId = <TError = unknown>(
  userId: number, options?: { swr?:SWRConfiguration<Awaited<ReturnType<typeof findByUserId>>, TError> & { swrKey?: Key, enabled?: boolean }, request?: SecondParameter<typeof apiFetch> }
) => {
  const {swr: swrOptions, request: requestOptions} = options ?? {}

  const isEnabled = swrOptions?.enabled !== false && userId !== null && userId !== undefined
  const swrKey = swrOptions?.swrKey ?? (() => isEnabled ? getFindByUserIdKey(userId) : null);
  const swrFn = () => findByUserId(userId, requestOptions)

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(swrKey, swrFn, swrOptions)

  return {
    swrKey,
    ...query
  }
}
export const create1 = (
    userId: number,
    createRatingRequest: CreateRatingRequest,
 options?: SecondParameter<typeof apiFetch>) => {
    return apiFetch<RatingResponse>(
    {url: `/api/users/${userId}/ratings`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: createRatingRequest
    },
    options);
  }



export const getCreate1MutationFetcher = (userId: number, options?: SecondParameter<typeof apiFetch>) => {
  return (_: Key, { arg }: { arg: CreateRatingRequest }) => {
    return create1(userId, arg, options);
  }
}
export const getCreate1MutationKey = (userId: number,) => [`/api/users/${userId}/ratings`] as const;

export type Create1MutationResult = NonNullable<Awaited<ReturnType<typeof create1>>>

export const useCreate1 = <TError = unknown>(
  userId: number, options?: { swr?:SWRMutationConfiguration<Awaited<ReturnType<typeof create1>>, TError, Key, CreateRatingRequest, Awaited<ReturnType<typeof create1>>> & { swrKey?: string }, request?: SecondParameter<typeof apiFetch>}
) => {

  const {swr: swrOptions, request: requestOptions} = options ?? {}

  const swrKey = swrOptions?.swrKey ?? getCreate1MutationKey(userId);
  const swrFn = getCreate1MutationFetcher(userId, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions)

  return {
    swrKey,
    ...query
  }
}
