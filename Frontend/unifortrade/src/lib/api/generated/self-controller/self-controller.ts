/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * UniforTrade API
 * Marketplace de economia circular do campus — estudantes anunciam itens (livros, calculadoras, componentes eletrônicos, jalecos, móveis etc.) para venda ou doação a outros estudantes.
 *
 * Fluxo de uma transação: uma listing ACTIVE é reivindicada (`POST /{id}/claim`, vira CLAIMED), as duas partes trocam contato (`GET /{id}/contact`) para combinar local e horário, e o vendedor confirma a entrega presencial (`POST /{id}/complete`). Qualquer uma das partes pode desistir antes da entrega (`DELETE /{id}/claim`), o que devolve a listing para ACTIVE.
 *
 * A maioria dos endpoints de leitura é pública; escrita e ações de fluxo exigem um usuário autenticado, e endpoints de categoria (além de GET) exigem a role ADMIN.
 *
 * Autenticação: o cadastro é `POST /api/users`, que cria a conta e retorna o perfil, mas não devolve tokens. Faça login em `POST /auth/login` para receber o par `accessToken` / `refreshToken`, renove em `POST /auth/refresh` e encerre a sessão no cliente com `POST /auth/logout`. Use o `accessToken` no botão "Authorize" acima para enviá-lo como `Authorization: Bearer <token>`.
 *
 * OpenAPI spec version: 0.0.1
 */
import useSwr from 'swr';
import type {
  Arguments,
  Key,
  SWRConfiguration
} from 'swr';

import useSWRMutation from 'swr/mutation';
import type {
  SWRMutationConfiguration
} from 'swr/mutation';

import type {
  CurrentUserResponse,
  PatchUserRequest
} from '../models';

import { apiFetch } from '../../mutator';



  type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];



 export const getSelf = (

 options?: SecondParameter<typeof apiFetch>) => {
    return apiFetch<CurrentUserResponse>(
    {url: `/api/users/me`, method: 'GET'
    },
    options);
  }



export const getGetSelfKey = () => [`/api/users/me`] as const;

export type GetSelfQueryResult = NonNullable<Awaited<ReturnType<typeof getSelf>>>

export const useGetSelf = <TError = unknown>(
   options?: { swr?:SWRConfiguration<Awaited<ReturnType<typeof getSelf>>, TError> & { swrKey?: Key, enabled?: boolean }, request?: SecondParameter<typeof apiFetch> }
) => {
  const {swr: swrOptions, request: requestOptions} = options ?? {}

  const isEnabled = swrOptions?.enabled !== false
  const swrKey = swrOptions?.swrKey ?? (() => isEnabled ? getGetSelfKey() : null);
  const swrFn = () => getSelf(requestOptions)

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(swrKey, swrFn, swrOptions)

  return {
    swrKey,
    ...query
  }
}
export const deleteSelf = (

 options?: SecondParameter<typeof apiFetch>) => {
    return apiFetch<void>(
    {url: `/api/users/me`, method: 'DELETE'
    },
    options);
  }



export const getDeleteSelfMutationFetcher = ( options?: SecondParameter<typeof apiFetch>) => {
  return (_: Key, __: { arg: Arguments }) => {
    return deleteSelf(options);
  }
}
export const getDeleteSelfMutationKey = () => [`/api/users/me`] as const;

export type DeleteSelfMutationResult = NonNullable<Awaited<ReturnType<typeof deleteSelf>>>

export const useDeleteSelf = <TError = unknown>(
   options?: { swr?:SWRMutationConfiguration<Awaited<ReturnType<typeof deleteSelf>>, TError, Key, Arguments, Awaited<ReturnType<typeof deleteSelf>>> & { swrKey?: string }, request?: SecondParameter<typeof apiFetch>}
) => {

  const {swr: swrOptions, request: requestOptions} = options ?? {}

  const swrKey = swrOptions?.swrKey ?? getDeleteSelfMutationKey();
  const swrFn = getDeleteSelfMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions)

  return {
    swrKey,
    ...query
  }
}
export const patchSelf = (
    patchUserRequest: PatchUserRequest,
 options?: SecondParameter<typeof apiFetch>) => {
    return apiFetch<CurrentUserResponse>(
    {url: `/api/users/me`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: patchUserRequest
    },
    options);
  }



export const getPatchSelfMutationFetcher = ( options?: SecondParameter<typeof apiFetch>) => {
  return (_: Key, { arg }: { arg: PatchUserRequest }) => {
    return patchSelf(arg, options);
  }
}
export const getPatchSelfMutationKey = () => [`/api/users/me`] as const;

export type PatchSelfMutationResult = NonNullable<Awaited<ReturnType<typeof patchSelf>>>

export const usePatchSelf = <TError = unknown>(
   options?: { swr?:SWRMutationConfiguration<Awaited<ReturnType<typeof patchSelf>>, TError, Key, PatchUserRequest, Awaited<ReturnType<typeof patchSelf>>> & { swrKey?: string }, request?: SecondParameter<typeof apiFetch>}
) => {

  const {swr: swrOptions, request: requestOptions} = options ?? {}

  const swrKey = swrOptions?.swrKey ?? getPatchSelfMutationKey();
  const swrFn = getPatchSelfMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions)

  return {
    swrKey,
    ...query
  }
}
