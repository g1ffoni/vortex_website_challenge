/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * UniforTrade API
 * Marketplace de economia circular do campus — estudantes anunciam itens (livros, calculadoras, componentes eletrônicos, jalecos, móveis etc.) para venda ou doação a outros estudantes.
 *
 * Fluxo de uma transação: uma listing ACTIVE é reivindicada (`POST /{id}/claim`, vira CLAIMED), as duas partes trocam contato (`GET /{id}/contact`) para combinar local e horário, e o vendedor confirma a entrega presencial (`POST /{id}/complete`). Qualquer uma das partes pode desistir antes da entrega (`DELETE /{id}/claim`), o que devolve a listing para ACTIVE.
 *
 * A maioria dos endpoints de leitura é pública; escrita e ações de fluxo exigem um usuário autenticado, e endpoints de categoria (além de GET) exigem a role ADMIN.
 *
 * Autenticação: o cadastro é `POST /api/users`, que cria a conta e retorna o perfil, mas não devolve tokens. Faça login em `POST /auth/login` para receber o par `accessToken` / `refreshToken`, renove em `POST /auth/refresh` e encerre a sessão no cliente com `POST /auth/logout`. Use o `accessToken` no botão "Authorize" acima para enviá-lo como `Authorization: Bearer <token>`.
 *
 * OpenAPI spec version: 0.0.1
 */
import type {
  Arguments,
  Key
} from 'swr';

import useSWRMutation from 'swr/mutation';
import type {
  SWRMutationConfiguration
} from 'swr/mutation';

import type {
  AuthResponse,
  LoginRequest,
  RefreshRequest
} from '../models';

import { apiFetch } from '../../mutator';



  type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];



 export const refresh = (
    refreshRequest: RefreshRequest,
 options?: SecondParameter<typeof apiFetch>) => {
    return apiFetch<AuthResponse>(
    {url: `/auth/refresh`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: refreshRequest
    },
    options);
  }



export const getRefreshMutationFetcher = ( options?: SecondParameter<typeof apiFetch>) => {
  return (_: Key, { arg }: { arg: RefreshRequest }) => {
    return refresh(arg, options);
  }
}
export const getRefreshMutationKey = () => [`/auth/refresh`] as const;

export type RefreshMutationResult = NonNullable<Awaited<ReturnType<typeof refresh>>>

export const useRefresh = <TError = unknown>(
   options?: { swr?:SWRMutationConfiguration<Awaited<ReturnType<typeof refresh>>, TError, Key, RefreshRequest, Awaited<ReturnType<typeof refresh>>> & { swrKey?: string }, request?: SecondParameter<typeof apiFetch>}
) => {

  const {swr: swrOptions, request: requestOptions} = options ?? {}

  const swrKey = swrOptions?.swrKey ?? getRefreshMutationKey();
  const swrFn = getRefreshMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions)

  return {
    swrKey,
    ...query
  }
}
export const logout = (

 options?: SecondParameter<typeof apiFetch>) => {
    return apiFetch<void>(
    {url: `/auth/logout`, method: 'POST'
    },
    options);
  }



export const getLogoutMutationFetcher = ( options?: SecondParameter<typeof apiFetch>) => {
  return (_: Key, __: { arg: Arguments }) => {
    return logout(options);
  }
}
export const getLogoutMutationKey = () => [`/auth/logout`] as const;

export type LogoutMutationResult = NonNullable<Awaited<ReturnType<typeof logout>>>

export const useLogout = <TError = unknown>(
   options?: { swr?:SWRMutationConfiguration<Awaited<ReturnType<typeof logout>>, TError, Key, Arguments, Awaited<ReturnType<typeof logout>>> & { swrKey?: string }, request?: SecondParameter<typeof apiFetch>}
) => {

  const {swr: swrOptions, request: requestOptions} = options ?? {}

  const swrKey = swrOptions?.swrKey ?? getLogoutMutationKey();
  const swrFn = getLogoutMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions)

  return {
    swrKey,
    ...query
  }
}
export const login = (
    loginRequest: LoginRequest,
 options?: SecondParameter<typeof apiFetch>) => {
    return apiFetch<AuthResponse>(
    {url: `/auth/login`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: loginRequest
    },
    options);
  }



export const getLoginMutationFetcher = ( options?: SecondParameter<typeof apiFetch>) => {
  return (_: Key, { arg }: { arg: LoginRequest }) => {
    return login(arg, options);
  }
}
export const getLoginMutationKey = () => [`/auth/login`] as const;

export type LoginMutationResult = NonNullable<Awaited<ReturnType<typeof login>>>

export const useLogin = <TError = unknown>(
   options?: { swr?:SWRMutationConfiguration<Awaited<ReturnType<typeof login>>, TError, Key, LoginRequest, Awaited<ReturnType<typeof login>>> & { swrKey?: string }, request?: SecondParameter<typeof apiFetch>}
) => {

  const {swr: swrOptions, request: requestOptions} = options ?? {}

  const swrKey = swrOptions?.swrKey ?? getLoginMutationKey();
  const swrFn = getLoginMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions)

  return {
    swrKey,
    ...query
  }
}
